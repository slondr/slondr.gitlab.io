---
title: Blog
layout: post
blog_index: true
---

<ul>
  {% for post in site.posts %}
  {% if post.draft != true %}
  <li>
    <div class="post-date"><i>{{ post.date | date: '%B %-d, %Y' }}</i></div>
    <a href="{{ post.url }}">{{ post.title }}</a>
  </li>
  {% endif}
  {% endfor}
</ul>
